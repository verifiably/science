# Task 2 report: Canonical inputs and the refusal envelope

## Implementation

- Added `python/src/science/canonical.py`: validates declared inputs, applies defaults, drops absent optionals, rejects unknown/wrong/null/missing values, and computes stable SHA-256 digests.
- Added `python/src/science/refusal.py`: the eight refusal codes, invocation-id grammar, frozen refusal value, exception carrier, wire envelope, and 32-byte token minting.
- Added `python/tests/test_canonical.py`: real-behavior tests for canonicalization, validation, digest stability, and refusal shapes.

## TDD evidence

RED command:

```text
cd python && uv run --group dev pytest tests/test_canonical.py -q
```

Relevant RED output:

```text
ModuleNotFoundError: No module named 'science.canonical'
!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
```

GREEN command:

```text
cd python && uv run --group dev pytest tests/test_canonical.py -q
```

Relevant GREEN output:

```text
......                                                                   [100%]
6 passed in 0.06s
```

Each test names the production break it catches: default/drop behavior, unknown/type/required validation, explicit-null handling, enum/list validation, order-independent digesting, and refusal protocol shape/token bounds.

## Final verification

```text
cd python && uv run --group dev pytest -q
........................................                                 [100%]
40 passed in 0.07s

tasks check --pretty
ok
```

## Self-review

The implementation is stdlib-only and limited to the requested boundary. It fails early with one structured `invalid-input` refusal path, excludes booleans from integer inputs, sorts canonical output and JSON keys for stable digests, and preserves optional invocation IDs on `Refused`. No compatibility layer, fallback, or unrelated file change was added. `python/uv.lock` was generated by the test command and is intentionally not part of this task.

## Fix Round 1

Review finding: closed input values must reject subclasses, including list elements.

RED command and output:

```text
uv run --group dev pytest tests/test_canonical.py::test_subclasses_are_refused_at_closed_input_boundary -q
F                                                                        [100%]
Failed: DID NOT RAISE Refused
```

GREEN commands and output:

```text
uv run --group dev pytest tests/test_canonical.py -q
.......                                                                  [100%]
7 passed in 0.01s

uv run --group dev pytest -q
.........................................                                [100%]
41 passed in 0.07s

tasks check --pretty
ok
```

Files: `python/src/science/canonical.py`, `python/tests/test_canonical.py`, this report, and the controller's existing `tasks/sci-88c112.md` review note. The fix uses exact `type(...) is ...` checks for every closed type and each list element. Commit: `fix(protocol): reject input type subclasses`.
